# API Fetch App

A minimal web application demonstrating **fetching data from text files, JSON files, and APIs**.

---

## Features

- Fetch and display a **text file**.  
- Fetch and display a **JSON file**.  
- Fetch and display data from an **external API** (example: JSONPlaceholder posts).  
